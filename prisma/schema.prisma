generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql" // or "mysql", "sqlite", "postgresql"
  url      = env("DATABASE_URL")
}

model Student {
  id                String    @id @default(cuid())
  firstName         String
  lastName          String
  email             String    @unique
  phoneNo           String
  slot              String
  courseName        String
  photoUrl          String?
  verificationCode  String?
  codeExpiry        DateTime?
  isVerified        Boolean   @default(false)
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@index([email])
}

